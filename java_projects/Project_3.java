public class Project_3 {

	public static void main(String[] args) {

		// Identify more than one variable fromt the same type at the same time without any value. 
		// For example:
		int a, b, c;

		// Identify more than one variable from the same type at the same time with any value
		// For example:
		int d=10, e=20, f=30;
		
		// Identify more than one variable from the same type at the same time without any value and have different nature
		// For example:
		int g, h[], i[][];
		
		// You can define an array in several ways
		// Way number one:
		int[] j;
		// Way number two:
		int []k;
		// Way number Three:
		int l[];

		// Define more than one array at the same time
		int[] n, s, z;

		// Declare a new variablie without any value
		int m;

		// Assign new value to a variable that is already exist
		a = 15;

		// Initialise a new variable or Create a new variable
		// For example:
		double Sum = 0;

	}

}
